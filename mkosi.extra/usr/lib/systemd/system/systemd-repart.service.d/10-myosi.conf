# 90-data.conf has no SizeMaxBytes, so once the disk is laid out repart
# exits 1 ("Can't fit requested partitions...") every boot. Whitelist it
# so the unit settles into active(exited); the initrd's own drop-in
# additionally adds After=sysroot.mount, not needed here.
[Service]
SuccessExitStatus=1
