[Partition]
# Runtime root-B verity-sig placeholder. Empty slot — no Verity=
# fields declared (per ParticleOS pattern); sysupdate writes them
# when filling the slot.
Type=root-verity-sig
Label=_empty
NoAuto=1
SplitName=-
# Weight=0 — fixed-size verity-sig partition, exclude from the
# proportional growth split. See 10-root-a-verity-sig.conf for the
# full rationale.
Weight=0
