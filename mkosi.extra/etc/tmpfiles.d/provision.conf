# Empty override masking upstream /usr/lib/tmpfiles.d/provision.conf
# (/etc wins by tmpfiles.d precedence). Upstream's `d /root` errors every
# boot because /root is a symlink to var/roothome on the read-only erofs
# rootfs, and its ssh.authorized_keys.root handling is replaced by our f^
# rule in tmpfiles.d/myosi.conf landing the key in /etc/ssh/authorized_keys.d.
