# Virt sysext — thin wrapper over mkosi.profiles/virt/: Packages= via
# the conf.d/10-packages.conf symlink (sub-image Include=/Profiles= don't
# propagate profile content in mkosi v26; conf.d auto-loads), mkosi.extra
# via ExtraTrees= below, sysext infra via conf.d/00-shared.conf,
# close-out in mkosi.postinst.

[Output]
ImageId=virt
Output=%i_%v_%a

[Content]
ExtraTrees=../../mkosi.profiles/virt/mkosi.extra:/
