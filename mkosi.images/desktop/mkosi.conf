# Desktop sysext — thin wrapper over mkosi.profiles/desktop/: Packages=
# via the conf.d/10-packages.conf symlink, mkosi.extra via ExtraTrees=
# below, sysext infra via conf.d/00-shared.conf, close-out (ffmpeg swap,
# finalize) in mkosi.postinst.

[Output]
ImageId=desktop
Output=%i_%v_%a

[Content]
ExtraTrees=../../mkosi.profiles/desktop/mkosi.extra:/
