# Same as the nvidia sysext's 50-myosi-nvidia.conf (keep in sync);
# per-branch filename so both sysexts don't shadow each other.
# Wants=, never Upholds= — the oneshot goes inactive on success and
# Upholds= would restart it forever.
[Unit]
Wants=nvidia-cdi-refresh.service nvidia-cdi-refresh.path
