# multi-user.target drop-in for the containers profile.
# incus-startup brings up boot.autostart instances. Upholds= (not
# .wants/) so a runtime sysext refresh auto-starts it without a reboot.
# No podman equivalent — rootless podman is per-user quadlets + linger.
[Unit]
Upholds=incus-startup.service
