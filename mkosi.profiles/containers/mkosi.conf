# Containers profile — podman + distrobox + skopeo + incus.
# Single source of truth: sysext wrapper (mkosi.images/containers), main
# image via --profile=containers, and mybox as a bake.

[Content]
# No podman-compose — `podman compose` is built into podman 5+.
Packages=podman
        distrobox
        skopeo
        incus
        incus-tools
