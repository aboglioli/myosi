# Containers profile — podman + distrobox + skopeo + incus.
# Single source of truth: sysext wrapper (mkosi.images/containers) and
# main image via --profile=containers.

[Content]
# No podman-compose — `podman compose` is built into podman 5+.
Packages=podman
        distrobox
        skopeo
        incus
        incus-tools
