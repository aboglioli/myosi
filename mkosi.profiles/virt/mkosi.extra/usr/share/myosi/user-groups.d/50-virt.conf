# virt feature — default user group bindings, consumed by
# /usr/libexec/myosi/homed-bind-sysext-groups. Single source of truth
# for baked AND sysext builds (sysext pulls it via ExtraTrees=) —
# do NOT duplicate under mkosi.images/virt/mkosi.extra/.

libvirt
