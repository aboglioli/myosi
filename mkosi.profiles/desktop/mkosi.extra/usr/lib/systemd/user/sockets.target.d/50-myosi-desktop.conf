# User-scope sockets.target drop-in for the desktop profile. Nothing in
# myosi runs `systemctl --user preset-all`, so without this Upholds=
# fresh users boot with pipewire + gnome-keyring disabled (no audio,
# no SSH agent).
[Unit]
Upholds=pipewire.socket pipewire-pulse.socket gnome-keyring-daemon.socket
