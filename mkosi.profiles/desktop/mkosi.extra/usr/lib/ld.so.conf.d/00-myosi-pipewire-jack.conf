# pipewire-jack's RPM puts its ld.so.conf.d entry in /etc, which sysexts
# don't ship — without this /usr/lib copy waybar (any JACK client) fails
# with "libjack.so.0: cannot open shared object file". refresh_sysext()
# must run ldconfig after merge for the cache to pick this up.
/usr/lib64/pipewire-0.3/jack
